/**
 * Presentation layer.
 *
 *   ui/      shadcn/ui primitives (generated — do not add business logic)
 *   layout/  App shell, navigation, page chrome
 *   journey/ Journey-specific presentational components (timeline, graph,
 *            recommendation cards, trust badges, audit trace)
 *
 * Components render props and raise events. They never reach into `@/db`,
 * `@/adapters`, or `@/agents` — enforced by an ESLint boundary.
 *
 * Populated in Phase 8.
 */
export {};
