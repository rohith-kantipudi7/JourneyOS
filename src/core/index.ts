/**
 * Domain core — framework-free business logic.
 *
 * Contains the Trust Kernel, Journey orchestration, deterministic decision
 * scoring, and shared primitives (Result, branded IDs). Nothing in this layer
 * may import from `src/app`, `src/components`, or any adapter implementation.
 *
 * Populated in Phase 1 (Domain Foundation).
 */
export {};
